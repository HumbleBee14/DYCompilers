# Shared Assets

Centralise reusable tools, scripts, and configs here.

- `templates/` — scaffolding for new projects so every compiler follows the same layout.
- `tools/` — language-agnostic utilities (formatters, linters, benchmarking scripts, CI helpers).

Document any tool you add so future projects can adopt it quickly.
